// Week 2 scope: login, token refresh, password reset controllers.
// Kept as an empty stub so the module structure (routes/controller/service/model)
// is consistent with vendors and products from day one.
module.exports = {};
