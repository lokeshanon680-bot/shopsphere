# Copy this file to .env (development) or .env.production (production)
# and fill in real values. Never commit the actual .env file.

NODE_ENV=development
PORT=5000

# Local MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/shopsphere_dev

# JWT
JWT_SECRET=123lokeshwaran123lokeahwaran
JWT_EXPIRES_IN=7d

# CORS - comma separated list of allowed origins
CORS_ORIGIN=http://localhost:3000
